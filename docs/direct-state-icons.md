# Логіка іконок станів у Direct Manager (колонка «Стан»)

## Поняття

### Новий запис
- **Новий запис** = перший платний запис клієнта (ще не було платних візитів).
- Визначається через **spent**: `spent === 0` або `spent == null` (трактуємо як 0).
- `spent` приходить з Altegio — загальна сума витрат клієнта.

### Перезапис
- **Перезапис** = новий платний запис створений у **букінг-день попереднього запису**.
- Тобто: `paidServiceRecordCreatedAt` (поточного запису) = букінгдата попереднього (attended) запису.
- У системі — поле `paidServiceIsRebooking` (enriched у records-grouping).

### Букінгдата
- **Букінгдата поточного запису** = `paidServiceDate` — дата запланованого візиту.
- **Зелений день** = букінгдата = сьогодні.
- **Червоний день** = букінгдата < сьогодні (минула дата).

---

## Логіка іконок (пріоритет від вищого до нижчого)

### 1. 🔥 Вогник (Продано)
- Консультація ✅ (`consultationAttended === true`)
- Є активний платний запис (`paidServiceDate` і не скасовано)
- **Новий запис** (`spent === 0` або `null`)
- `paidServiceDate >= сьогодні` (сьогодні або майбутнє)
- Не скасовано (`!paidServiceCancelled`), не no-show (`paidServiceAttended !== false`)

**Смисл:** Перший платний запис — продаж з консультації.

---

### 2. 🔁 Перезапис
- Консультація ✅
- Є платний запис (не скасовано, не no-show)
- **Дата створення поточного запису = букінгдата попереднього запису** (`paidServiceIsRebooking === true`)
- Букінгдата поточного запису = сьогодні або майбутнє

**Ключова умова:** `paidServiceRecordCreatedAt` (поточного) = `paidServiceDate` (попереднього attended). У системі — `paidServiceIsRebooking`.

**Смисл:** Новий платний запис створений у букінг-день попереднього (перезапис).

---

### 3. ⚠️ Жовтий трикутник (букінгдата в минулому)
- Є платний запис
- **Букінгдата поточного запису < сьогодні** (червоний день)
- Не скасовано, не no-show

**Смисл:** Запис у минулому — потребує уваги.

---

### 4. ⏳ Очікування (букінгдата в майбутньому)
- Є платний запис
- **Букінгдата поточного запису > сьогодні** (майбутнє)
- Винятки: 🔥 Продаж (новий запис), 🔁 Перезапис

**Смисл:** Очікуємо візит у майбутньому.

---

### 5. Інші стани
- 💔 Не продали — консультація ✅, немає запису на платну
- 🗓️ Синій календар — лише консультація (немає платного запису)

**Примітка:** Стани 🚫 (скасовано) та ❌ (no-show) у колонці «Стан» не показуються — логика видалена.

---

## Порядок перевірок у коді

1. Червона дата (paidServiceDate < сьогодні), не no-show, не cancelled → ⚠️
2. Зелена/майбутня дата + spent=0 (новий запис) → 🔥
3. Зелена/майбутня дата + paidServiceIsRebooking → 🔁
4. Майбутня дата (без 🔥, 🔁) → ⏳
5. Інші стани…

---

## Файли

- `web/app/admin/direct/_components/DirectClientTable.tsx` — колонка «Стан», рендер іконок
- `web/lib/altegio/records-grouping.ts` — обчислення `paidServiceIsRebooking`
- `web/app/api/admin/direct/clients/route.ts` — передача `spent`, `paidServiceIsRebooking`
- `web/lib/direct-types.ts` — типи `paidServiceIsRebooking`, `paidServiceRebookFromKyivDay`
