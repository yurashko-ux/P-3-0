import { ContactsPageContent } from '@/components/fund/fund-site';

export const metadata = {
  title: 'Контакти',
};

export default function FundContactsPage() {
  return <ContactsPageContent />;
}
