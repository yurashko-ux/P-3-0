// layout для /admin/direct — maxDuration для Server Actions (експорт IG #68)

export const maxDuration = 300;

export default function DirectAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
